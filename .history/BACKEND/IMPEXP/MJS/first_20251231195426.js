
import sum from './second.js';
console.log("This is the second file for Import and Export backend");

// here we can use .mjs extenstion also instead of .js if we are not using package.json file with type module
// because 